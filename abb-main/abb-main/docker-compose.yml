version: '3.8'

services:
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    ports:
      - '4200:80'  # Changed from 4200:4200 to 4200:80

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - '5000:8080'  # Changed from 5000:80 to 5000:8080

  ml_service:
    build:
      context: ./ml_service
      dockerfile: Dockerfile
    ports:
      - '8000:8000'